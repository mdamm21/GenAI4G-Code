# Core dependencies
anthropic>=0.54.0
python-dotenv>=1.0.0
pydantic>=2.0.0

# MCP server (Model Context Protocol)
mcp[cli]>=1.0.0
anyio>=4.0.0

# DeepAgent orchestration (optional)
# cnc.agent attempts `from deepagents import create_deep_agent` at import time.
# If unavailable, it falls back silently to the built-in CNCAgent (Anthropic API).
# Uncomment the line below once the correct package name is confirmed:
# deepagents>=0.1.0
#
# Environment variables used by cnc.agent:
#   CNC_AGENT_MODEL   — model for deepagents (default: openai:gpt-4o)
#   GENAI4G_MODEL     — model for built-in CNCAgent (default: claude-sonnet-4-6)
#   GENAI4G_MAX_TURNS — max agent loop turns (default: 20)
#   ANTHROPIC_API_KEY — required for built-in CNCAgent

# LangChain (used by scripts/cnc_milling.py and agent.py ReAct agent)
langchain>=0.3.0
langchain-anthropic>=0.3.0
langchain-mcp-adapters>=0.1.0
langgraph>=0.2.0
